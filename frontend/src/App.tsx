import ChatWidget from './components/ChatWidget';

/**
 * Composant racine de l'application.
 * Affiche le chatbot de génération de leads.
 */
const App = () => {
  return <ChatWidget />;
};

export default App;